# Changelog

## 0.2.0

- 新增显式参数配置，生成与回放保留 `${name}` 引用。
- 新增 `@rule` 动态定位：唯一范围、精确文本/数字后缀、唯一/首项/序号和最小数量。
- 新增 `replay` 命令：PB/RDC 目录扫描、逐文件独立回放、运行参数注入和 JSON 汇总。
- 收紧步骤完成校验：显式引用不得固化，必需断言不得因重试次数放行。
- 回放保存路径同样验证边界，命令参数仅允许安全标识符字符。
- 新增详细迁移与描述改写指南，说明真实 Electron 应用仍需验证的边界。
