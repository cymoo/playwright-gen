{
  "name": "playwright-gen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "从 URL 或 Electron 应用 + 自然语言描述，自动生成可运行的 Playwright (TypeScript) 测试",
  "scripts": {
    "pwgen": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "playwright test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.2",
    "@playwright/test": "^1.61.1",
    "ai": "^7.0.9",
    "commander": "^15.0.0",
    "dotenv": "^17.4.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
